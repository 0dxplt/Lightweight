import { BetterMsViewerPipe } from './better-ms-viewer-pipe';

describe('BetterMsViewerPipe', () => {
  it('create an instance', () => {
    const pipe = new BetterMsViewerPipe();
    expect(pipe).toBeTruthy();
  });
});
